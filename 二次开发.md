# FinSight 二次开发指南

## 项目架构速览

```
FinSight/
├── app/main.py              # FastAPI 入口：路由挂载 + 静态文件
├── app/core/config.py       # 环境变量配置
├── app/core/glm_client.py   # ★ 核心：LLM 调用 + MCP 工具 + 错误处理
├── app/api/chat.py          # API 路由层（仅参数校验和分发）
├── app/static/
│   ├── index.html           # 主对话页面（所有交互逻辑在 HTML 内）
│   ├── models.html          # 模型文档页
│   ├── help.html            # 帮助文档页
│   └── assets/              # CSS/JS/图标
└── requirements.txt
```

---

## 开发环境搭建

```bash
git clone https://github.com/YOUR_USERNAME/FinSight.git
cd FinSight
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# 编辑 .env 填入 API Key

# 启动开发服务器（自带热重载）
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

---

## 核心模块说明

### app/core/glm_client.py — LLM 客户端核心

这是最关键的模块，也是二次开发最常改动的文件。

| 函数/变量 | 职责 |
|-----------|------|
| `stream_finance_chat()` | 金融对话流式生成器 |
| `stream_vision_chat()` | 图像分析流式生成器 |
| `MCP_SERVERS` | 广发证券 MCP 服务地址列表 |
| `_build_mcp_tools()` | 构建 MCP 工具列表 |
| `_friendly_error()` | 错误码转中文提示 |
| `ERROR_CODE_MAP` | 智谱业务错误码映射 |

**数据流：**

```
用户输入 → chat.py → glm_client.py → Zhipu SDK → 智谱 API
    → SDK 返回流式 chunk → yield SSE 事件 → StreamingResponse → 前端
```

### app/api/chat.py — API 路由

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/chat/stream` | POST | SSE 流式对话，支持 finance/vision 两种模式 |

### 前端 index.html

所有交互在单个 HTML 中，关键函数：

| 函数 | 职责 |
|------|------|
| `sendMessage()` | 核心：fetch SSE → 流式渲染 |
| `switchMode(mode)` | 切换 finance/vision 模式 |
| `addStreamingAiMessage()` | 创建流式消息 DOM 容器 |
| `renderMarkdown()` | marked + DOMPurify 渲染 |

---

## 常见二次开发场景

### 1. 更换 LLM 提供商

以切换到 OpenAI 为例：

- `requirements.txt`：移除 `zai-sdk`，添加 `openai`
- 新增 `app/core/llm_client.py`，使用 OpenAI SDK
- 修改 `app/api/chat.py` 中的 import
- MCP 工具（`type: "mcp"`）是 Zhipu SDK 特有功能，新厂商需改为 Function Calling 方式

### 2. 新增 MCP 工具

在 `glm_client.py` 的 `MCP_SERVERS` 列表中添加新服务地址，同时更新系统提示词告知模型新工具的能力。

### 3. 新增聊天模式

后端添加新的 `stream_xxx_chat()` 函数，API 层增加模式判断，前端添加切换按钮。

---

## 调试技巧

```bash
# 用 curl 模拟 SSE 流式请求
curl -N -X POST http://localhost:8000/api/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"你好"}],"mode":"finance"}'

# 无效的 GFZQ_TOKEN 会自动降级为无工具模式，方便调试 LLM 本身
```

**浏览器 Console 全局变量：**

```javascript
messages        // 当前对话消息数组
currentMode     // 当前模式
thinkingEnabled // 深度思考开关
conversations   // 全部对话列表
```
